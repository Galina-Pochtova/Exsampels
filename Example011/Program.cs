// new int [10] - сщздай новый массив, в которм 10 элементов (ни чего не поняла в это коде)
/*void FillArray(int[ collection])
{
    int length = collection.Length;
    int index = 0;
    while (index < length)
    {
        collection[index] = new Random().Next(1,10);
        index++;
    }
}
int[] array = new int [10];
*/
void FillArray(int[ collection])
 
 void PrintArray(int[] col)

 int[] array = new int[10];

 FillArray(array);
 PrintArray(array);

